name: Deploy — PWA + Supabase Functions

on:
  push:
    branches: [master, main]
    paths:
      - 'index.html'
      - 'manifest.json'
      - 'sw.js'
      - 'icons/**'
      - 'supabase/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

concurrency:
  group: deploy-pwa
  cancel-in-progress: true

jobs:
  deploy-pwa:
    name: Deploy PWA → app.zepo.lynoia.com
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare deploy folder
        run: |
          mkdir -p _deploy
          cp index.html _deploy/
          cp manifest.json _deploy/
          cp sw.js _deploy/
          cp -r icons _deploy/

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy _deploy --project-name=zepo --branch=main --commit-dirty=true

  deploy-functions:
    name: Deploy Edge Functions → Supabase
    runs-on: ubuntu-latest
    # Solo si hay cambios en supabase/ (modified o added) o manual
    if: |
      contains(join(github.event.head_commit.modified, ','), 'supabase/') ||
      contains(join(github.event.head_commit.added, ','), 'supabase/') ||
      github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy save-push-sub \
            --project-ref vchaxqisbypwwtyjjnjr \
            --no-verify-jwt
          supabase functions deploy send-daily-push \
            --project-ref vchaxqisbypwwtyjjnjr \
            --no-verify-jwt
          supabase functions deploy send-weekly-email \
            --project-ref vchaxqisbypwwtyjjnjr \
            --no-verify-jwt
        working-directory: supabase/functions

      - name: Set Edge Function secrets
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          echo "xb579mNED3fUmBGPIeA2FUlXIi6yBpTI24ZpLrx3890" | \
            supabase secrets set VAPID_PRIVATE_KEY --project-ref vchaxqisbypwwtyjjnjr
          echo "BFarnYqUAFW5QEds5F0JQoL4BBpkApdV0hi9unggIHSky4wLmOJgEpIQEGVbHIJ36pL0kvv14mOlYD8orYzqXAM" | \
            supabase secrets set VAPID_PUBLIC_KEY --project-ref vchaxqisbypwwtyjjnjr
          echo "hola@zepo.app" | \
            supabase secrets set VAPID_EMAIL --project-ref vchaxqisbypwwtyjjnjr
          echo "${{ secrets.RESEND_API_KEY }}" | \
            supabase secrets set RESEND_API_KEY --project-ref vchaxqisbypwwtyjjnjr
          echo "Zepo <hola@zepo.lynoia.com>" | \
            supabase secrets set FROM_EMAIL --project-ref vchaxqisbypwwtyjjnjr
